%ul.nav.mainmenu
  %li.divider-vertical
  %li{class: (:active if active_page?('admin/episodes'))}
    = link_to 'Episodes', [:admin, :episodes]
  %li{class: (:active if active_page?('admin/interactions'))}
    = link_to 'Interaction', [:admin, :interactions]
  %li{class: (:active if active_page?('admin/programs') || active_page?('admin/images'))}
    = link_to 'Programs', [:admin, :programs]
  %li{class: (:active if active_page?('admin/users'))}
    = link_to 'Users', [:admin, :users]
