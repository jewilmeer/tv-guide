= semantic_form_for @user do |f|
  = f.inputs do
    = f.input :email
    = f.input :password
    = f.input :password_confirmation
    = f.input :login
  
  = f.buttons do
    = f.commit_button :button_html => { :class => :primary, :data => {:'disable-with' => 'Please wait...'} }
