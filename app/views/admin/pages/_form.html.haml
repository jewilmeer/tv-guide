= form_for([:admin, @page]) do |f|
  .form_row
    = f.label :title
    = f.text_field :title
  .form_row
    = f.label :permalink
    = f.text_field :permalink
  .form_row
    = f.label :content
    = f.text_area :content
  = f.submit "#{@page.new_record? ? 'Add' : 'Update'} page"