$('tbody tr:last').after('<%= j render(@episodes) %>');
<% if @episodes.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate(@episodes) %>');
<% else %>
  $('.pagination').remove();
<% end %>
