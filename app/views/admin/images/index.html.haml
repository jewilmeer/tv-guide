%section#images
  .page-header
    %h1 Images overview

  %ul.media-grid
    - @images.each do |image|
      %li[image]
        = link_to [:admin, image] do
          %img.thumbnail{:src => "/images/#{image.id}.jpg"}