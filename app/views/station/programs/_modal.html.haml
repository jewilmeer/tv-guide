.modal.hide.fade
  .modal-header
    %button.close{'data-dismiss' => 'modal', "aria-hidden" => true} &times;
    %h3 Add new program
  = simple_form_for @station_program, url: station_programs_path(@station) do |f|
    .modal-body
      = f.association :program, collection: Program.active.order('status, programs.name')
    .modal-footer
      = f.button :submit, as: :submit, class: 'btn btn-primary'
